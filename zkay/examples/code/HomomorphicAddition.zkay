pragma zkay >=0.2.0;

contract HomomorphicAddition {

	final address owner;
	uint@owner<+> a;
	uint@owner<+> b;
	uint@owner<+> sum;
	uint@owner<+> diff;

	constructor(uint@me _a, uint@me _b) public {
		owner = me;
		a = _a;
		b = _b;
	}

	function f() public {
		sum = a + b;
		diff = a - b;
	}
}
